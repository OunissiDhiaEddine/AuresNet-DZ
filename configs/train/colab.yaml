max_epochs: 100
learning_rate: 0.0002
weight_decay: 0.00001

accelerator: gpu
devices: 1
num_nodes: 1
strategy: auto
precision: 16-mixed
accumulate_grad_batches: 2
gradient_clip_val: 1.0
deterministic: false

log_every_n_steps: 10
checkpoint_dir: /content/drive/MyDrive/AuresNet-DZ/checkpoints
early_stopping_patience: 10
